# 🚀 Деплой на хостинг .by

## 📋 Подготовка к деплою

### 1. Что нужно от хостинга

- ✅ Node.js 18+ (проверьте: `node --version`)
- ✅ npm или yarn
- ✅ Доступ по SSH или FTP/SFTP
- ✅ Минимум 512 MB RAM
- ✅ Порты 3000+ доступны (или настройте прокси через веб-сервер)

---

## 🔧 Способ 1: Деплой через SSH (VPS/выделенный сервер)

### Шаг 1: Подготовка проекта

```bash
# На локальной машине - создайте production сборку
npm run build

# Упакуйте файлы для загрузки (без node_modules и .next)
# Или используйте Git
```

### Шаг 2: Загрузка на сервер

**Вариант А: Через Git (рекомендуется)**

```bash
# На сервере
cd /var/www/html  # или ваш путь
git clone https://github.com/ваш-репозиторий/excavator.git
cd excavator
npm install --production
npm run build
```

**Вариант Б: Через FTP/SFTP**

1. Загрузите все файлы проекта на сервер
2. Исключите: `node_modules`, `.next`, `.git`
3. После загрузки на сервере выполните:
   ```bash
   npm install --production
   npm run build
   ```

### Шаг 3: Настройка переменных окружения

```bash
# На сервере создайте .env файл
nano .env
```

Добавьте:
```env
NODE_ENV=production
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_CHAT_ID=ваш_chat_id
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
NEXT_PUBLIC_YANDEX_COUNTER_ID=XXXXXXXX
PORT=3000
```

### Шаг 4: Запуск с PM2 (рекомендуется)

```bash
# Установите PM2
npm install -g pm2

# Запустите приложение
pm2 start npm --name "excavator-lida" -- start

# Сохраните конфигурацию
pm2 save

# Настройте автозапуск при перезагрузке
pm2 startup
# Выполните команду, которую покажет PM2

# Проверьте статус
pm2 status
pm2 logs excavator-lida
```

### Шаг 5: Настройка Nginx (прокси)

```nginx
server {
    listen 80;
    server_name excavator-lida.by www.excavator-lida.by;

    # Редирект на HTTPS (если есть SSL)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Кэширование статических файлов
    location /_next/static {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 200 60m;
        add_header Cache-Control "public, immutable";
    }

    # Изображения
    location /img {
        proxy_pass http://localhost:3000;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

**Примените конфигурацию:**
```bash
sudo nginx -t  # Проверка конфигурации
sudo systemctl reload nginx  # Перезагрузка Nginx
```

### Шаг 6: Настройка SSL (HTTPS)

```bash
# Установите Certbot
sudo apt-get update
sudo apt-get install certbot python3-certbot-nginx

# Получите сертификат
sudo certbot --nginx -d excavator-lida.by -d www.excavator-lida.by

# Автообновление
sudo certbot renew --dry-run
```

---

## 🌐 Способ 2: Shared Hosting с Node.js

### Если хостинг поддерживает Node.js через панель управления:

1. **Загрузите файлы** через FTP
2. **В панели хостинга:**
   - Настройте Node.js приложение
   - Укажите точку входа: `server.js` или `npm start`
   - Настройте переменные окружения через панель
3. **Запустите приложение**

---

## 📦 Способ 3: Статический экспорт (без Node.js)

Если хостинг не поддерживает Node.js, можно использовать статический экспорт:

### 1. Настройте next.config.js:

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
```

### 2. Соберите статический сайт:

```bash
npm run build
```

### 3. Загрузите папку `out/` на хостинг через FTP

**⚠️ Ограничения статического экспорта:**
- ❌ API routes не работают (формы в Telegram не будут работать напрямую)
- ❌ Server-Side Rendering отключен
- ✅ Можно использовать внешний сервис для форм (например, FormSubmit, Netlify Forms)

---

## 🔄 Обновление после изменений

```bash
# На сервере
cd /path/to/excavator
git pull  # Если используете Git
# или загрузите новые файлы через FTP

npm install --production
npm run build
pm2 restart excavator-lida
```

---

## 📊 Мониторинг и логи

### PM2 команды:

```bash
pm2 status              # Статус приложений
pm2 logs excavator-lida # Просмотр логов
pm2 monit              # Мониторинг в реальном времени
pm2 restart excavator-lida  # Перезапуск
pm2 stop excavator-lida     # Остановка
pm2 delete excavator-lida   # Удаление
```

### Nginx логи:

```bash
sudo tail -f /var/log/nginx/access.log  # Логи доступа
sudo tail -f /var/log/nginx/error.log   # Логи ошибок
```

---

## ✅ Чеклист после деплоя

- [ ] Сайт открывается по домену
- [ ] HTTPS работает (SSL сертификат)
- [ ] Формы отправляются (проверьте Telegram)
- [ ] Изображения загружаются
- [ ] Мобильная версия работает
- [ ] Аналитика работает (если настроена)
- [ ] PM2 настроен на автозапуск
- [ ] Nginx настроен правильно
- [ ] Логи не показывают ошибок

---

## 🆘 Решение проблем

### Приложение не запускается:

```bash
# Проверьте логи
pm2 logs excavator-lida

# Проверьте порт
netstat -tlnp | grep 3000

# Проверьте переменные окружения
cat .env
```

### Ошибка 502 Bad Gateway:

- Проверьте, что приложение запущено: `pm2 status`
- Проверьте порт в Nginx конфигурации
- Проверьте логи Nginx: `sudo tail -f /var/log/nginx/error.log`

### Ошибки памяти:

```bash
# Увеличьте лимит памяти для Node.js в PM2
pm2 restart excavator-lida --update-env --node-args="--max-old-space-size=512"
```

---

## 📞 Полезные команды

```bash
# Проверка версии Node.js
node --version

# Проверка процессов Node.js
ps aux | grep node

# Проверка использования портов
sudo netstat -tlnp | grep :3000

# Перезапуск всего стека
pm2 restart all
sudo systemctl reload nginx
```

---

**Успешного деплоя!** 🚀

