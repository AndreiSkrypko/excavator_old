# ⚡ Быстрый деплой на хостинг .by

## 🚀 Самый простой способ (через SSH + PM2)

### 1. Подготовка на сервере

```bash
# Подключитесь к серверу по SSH
ssh user@ваш-сервер.by

# Перейдите в директорию сайта
cd /var/www/html  # или ваш путь

# Клонируйте репозиторий (или загрузите файлы через FTP)
git clone https://github.com/ваш-репозиторий/excavator.git
cd excavator

# Или если загружали через FTP, просто перейдите в папку проекта
```

### 2. Установка зависимостей

```bash
npm install --production
```

### 3. Настройка переменных окружения

```bash
nano .env
```

Добавьте:
```env
NODE_ENV=production
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_CHAT_ID=ваш_chat_id
PORT=3000
```

### 4. Сборка проекта

```bash
npm run build
```

### 5. Запуск с PM2

```bash
# Установите PM2 (если еще не установлен)
npm install -g pm2

# Запустите приложение
pm2 start npm --name "excavator-lida" -- start

# Сохраните конфигурацию
pm2 save

# Настройте автозапуск
pm2 startup
# Выполните команду, которую покажет PM2
```

### 6. Настройка Nginx

```bash
sudo nano /etc/nginx/sites-available/excavator-lida.by
```

Скопируйте содержимое из `nginx.conf.example`, измените `server_name` на ваш домен.

```bash
# Активируйте сайт
sudo ln -s /etc/nginx/sites-available/excavator-lida.by /etc/nginx/sites-enabled/

# Проверьте конфигурацию
sudo nginx -t

# Перезагрузите Nginx
sudo systemctl reload nginx
```

### 7. Настройка SSL (HTTPS)

```bash
sudo apt-get install certbot python3-certbot-nginx
sudo certbot --nginx -d excavator-lida.by -d www.excavator-lida.by
```

---

## ✅ Готово!

Сайт должен быть доступен по адресу: **https://excavator-lida.by**

---

## 🔄 Обновление после изменений

```bash
cd /path/to/excavator
git pull  # или загрузите новые файлы
npm install --production
npm run build
pm2 restart excavator-lida
```

---

## 📋 Чеклист

- [ ] Node.js 18+ установлен
- [ ] Файлы загружены на сервер
- [ ] `.env` файл настроен
- [ ] `npm run build` выполнен успешно
- [ ] PM2 запущен
- [ ] Nginx настроен
- [ ] SSL сертификат установлен
- [ ] Сайт открывается
- [ ] Формы работают

---

**Подробная инструкция**: См. `DEPLOY_BY_HOSTING.md`

