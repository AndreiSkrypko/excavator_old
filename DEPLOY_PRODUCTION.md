# 🚀 Запуск в продакшене

## Локальный запуск production сборки

### 1. Сборка проекта

```bash
npm run build
```

Эта команда:
- Создаст оптимизированную production сборку
- Проверит код на ошибки
- Оптимизирует изображения и статические файлы
- Создаст статические HTML страницы где возможно

### 2. Запуск production сервера

```bash
npm start
```

После этого проект будет доступен на **http://localhost:3000**

---

## 🌐 Деплой на хостинг

### Vercel (Рекомендуется - самый простой способ)

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Задеплойте:**
   ```bash
   vercel
   ```
   
   Или через веб-интерфейс:
   - Зайдите на https://vercel.com
   - Подключите GitHub/GitLab/Bitbucket репозиторий
   - Vercel автоматически определит Next.js и задеплоит

3. **Настройте переменные окружения:**
   - В настройках проекта Vercel
   - Добавьте все переменные из `.env.local`:
     - `TELEGRAM_BOT_TOKEN`
     - `TELEGRAM_CHAT_ID`
     - `NEXT_PUBLIC_GA_MEASUREMENT_ID`
     - `NEXT_PUBLIC_YANDEX_COUNTER_ID`

**Преимущества Vercel:**
- ✅ Бесплатный хостинг
- ✅ Автоматический HTTPS
- ✅ Автоматические деплои при push в Git
- ✅ CDN из коробки
- ✅ Отлично работает с Next.js

---

### Netlify

1. **Установите Netlify CLI:**
   ```bash
   npm i -g netlify-cli
   ```

2. **Соберите проект:**
   ```bash
   npm run build
   ```

3. **Задеплойте:**
   ```bash
   netlify deploy --prod
   ```

Или через веб-интерфейс:
- Зайдите на https://app.netlify.com
- Перетащите папку `.next` (после `npm run build`)
- Настройте переменные окружения

---

### Docker (для VPS/сервера)

1. **Создайте `Dockerfile`:**
   ```dockerfile
   FROM node:18-alpine AS base

   # Install dependencies only when needed
   FROM base AS deps
   RUN apk add --no-cache libc6-compat
   WORKDIR /app
   COPY package.json package-lock.json ./
   RUN npm ci

   # Rebuild the source code only when needed
   FROM base AS builder
   WORKDIR /app
   COPY --from=deps /app/node_modules ./node_modules
   COPY . .
   RUN npm run build

   # Production image, copy all the files and run next
   FROM base AS runner
   WORKDIR /app
   ENV NODE_ENV production
   RUN addgroup --system --gid 1001 nodejs
   RUN adduser --system --uid 1001 nextjs

   COPY --from=builder /app/public ./public
   COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
   COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static

   USER nextjs
   EXPOSE 3000
   ENV PORT 3000
   CMD ["node", "server.js"]
   ```

2. **Соберите Docker образ:**
   ```bash
   docker build -t excavator-lida .
   ```

3. **Запустите контейнер:**
   ```bash
   docker run -p 3000:3000 -e TELEGRAM_BOT_TOKEN=ваш_токен -e TELEGRAM_CHAT_ID=ваш_id excavator-lida
   ```

---

### VPS/Сервер (Node.js)

1. **На сервере установите Node.js 18+:**
   ```bash
   # Ubuntu/Debian
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

2. **Загрузите проект на сервер** (через Git, FTP, SCP)

3. **Установите зависимости и соберите:**
   ```bash
   npm install
   npm run build
   ```

4. **Создайте `.env` файл** с переменными окружения

5. **Запустите с PM2 (рекомендуется):**
   ```bash
   # Установите PM2
   npm install -g pm2
   
   # Запустите приложение
   pm2 start npm --name "excavator-lida" -- start
   
   # Сохраните конфигурацию
   pm2 save
   pm2 startup
   ```

6. **Настройте Nginx (опционально, для прокси):**
   ```nginx
   server {
       listen 80;
       server_name excavator-lida.by;

       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

---

## ⚙️ Важные настройки для продакшена

### 1. Переменные окружения

Убедитесь, что все переменные окружения настроены:

```env
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_CHAT_ID=ваш_chat_id
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX (если используете)
NEXT_PUBLIC_YANDEX_COUNTER_ID=XXXXXXXX (если используете)
NODE_ENV=production
```

### 2. Проверка перед деплоем

```bash
# Проверьте сборку локально
npm run build

# Запустите production сервер локально
npm start

# Проверьте что все работает:
# - Формы отправляются
# - Изображения загружаются
# - Все ссылки работают
```

### 3. Оптимизация

- ✅ Убедитесь что все изображения оптимизированы
- ✅ Проверьте размер bundle (должен быть разумным)
- ✅ Настройте кэширование
- ✅ Настройте HTTPS (обязательно для продакшена)

---

## 📊 Мониторинг после деплоя

После деплоя проверьте:

1. ✅ Сайт открывается
2. ✅ Формы работают
3. ✅ Изображения загружаются
4. ✅ Аналитика работает (если настроена)
5. ✅ SEO мета-теги на месте (проверьте через View Source)
6. ✅ Мобильная версия работает
7. ✅ Скорость загрузки (через PageSpeed Insights)

---

## 🔧 Troubleshooting

### Ошибки при сборке

```bash
# Очистите кэш и пересоберите
rm -rf .next node_modules
npm install
npm run build
```

### Проблемы с переменными окружения

- Убедитесь что переменные правильно названы
- `NEXT_PUBLIC_*` переменные должны быть доступны на клиенте
- Остальные переменные только на сервере

### Проблемы с производительностью

- Используйте Next.js Image компонент для изображений
- Включите кэширование
- Используйте CDN для статических файлов

---

**Рекомендация**: Начните с Vercel - это самый простой и быстрый способ задеплоить Next.js приложение! 🚀

